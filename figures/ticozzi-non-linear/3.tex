% gallery-title: Circuit
% gallery-category: Nonlinear 
% gallery-tags: graph
% gallery-description: Figure from Francesco Ticozzi's book "Nonlinear systems and control"
% gallery-order: 10

\documentclass{standalone}[12pt]
\usepackage{circuitikz}
\usepackage{subcaption}
\newcommand*{\equal}{=}

\input{../commands}

\usetikzlibrary{shapes,arrows,decorations.markings}
\usetikzlibrary{calc,patterns,angles,quotes,arrows,automata}
\usetikzlibrary{positioning}

\begin{document}

% \begin{figure}\centering
%     \begin{subfigure}{.49\textwidth}\centering
        \begin{circuitikz}[american, voltage shift = 0.5] \draw
            (0,4) to[C=$C$,i>=$i_C$] (0,0) 
            (0,4) -- (2,4) 
            (2,4) to[L=$L$,i>=$i_L$] (2,0) -- (0,0)
            (2,4) -- (4,4) to[generic, l={\hspace{-3.5ex}$h$},i>=$i\equal h(v)$,v={\hspace{-5ex}$v$}] (4,0) -- (2,0) ;
        \end{circuitikz}	
    % \end{subfigure}
    % \begin{subfigure}{.49\textwidth}\centering
        \begin{tikzpicture}
            \pgfmathsetmacro{\max}{3}
            \draw[->] (-\max,0) -- (\max,0) node[right] {$v$};
            \draw[->] (0,-\max) -- (0,\max) node[above] {$h(v)$};
            
            \draw[scale=0.5,domain=-\max:\max,smooth,variable=\x,blue] plot ({\x},{-\x+\x^3/3});
        \end{tikzpicture}
    % \end{subfigure}
    % \begin{subfigure}{.49\textwidth}
        \begin{tikzpicture}\centering
            \draw[->] (0,0) -- (5,0) node[right] {$t$};
            \draw[->] (0,-2) -- (0,2) node[above] {$v(t)$};
            
            \draw[scale=0.5,domain=0:10,smooth,variable=\x,blue] plot ({\x},{cos(deg((\x))});
            \draw[scale=0.5,domain=0:10,smooth,variable=\x,red,dashed] plot ({\x},{(1-exp(-\x))*cos(deg((\x))});
            \draw[scale=0.5,domain=0:10,smooth,variable=\x,red,dashed] plot ({\x},{(1-3*exp(-\x))*cos(deg((\x))});
            \draw[scale=0.5,domain=0:10,smooth,variable=\x,red,dashed] plot ({\x},{(1+exp(-\x))*cos(deg((\x))});
        \end{tikzpicture}
%     \end{subfigure}
% \end{figure}
\\\bigskip

\vspace{1cm}

\end{document}